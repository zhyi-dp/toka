fn main() {
    // 1. Arithmetic Type Mismatch
    let i = 1 + 2.0; // Error: operands must be same type

    // 2. Comparison Type Mismatch
    let c = 1 == "s"; // Error: operands must be same type

    // 3. If Condition Type Mismatch
    if (1) {} // Error: condition must be bool

    // 4. Dereference Non-Pointer
    let x = 10;
    let y = *x; // Error: cannot dereference non-pointer

    // 5. Variable Type Mismatch
    let z: i32 = "hello"; // Error: mismatch
}

shape Buffer<T, N_: usize>(
    data: [T; N_],
    len: usize
)

fn buf_len(buf: Buffer<i32, 16>) -> usize {
    return buf.len
}

fn main() -> i32 {
    auto b = Buffer<i32, 16>(data = [0; 16], len = 0)
    buf_len(b)
    return 0
}

import core/io::println

fn main() -> i32 {
    auto x! = 10 // Nullable integer (not typical in Toka but testing the mechanism)
    if x! is x {
        println("x is not null: {}", x)
    }
    return 0
}

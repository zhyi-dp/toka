fn fib(n: i64) -> i64 {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> bool {
    auto start_n = 45:i64
    auto res = fib(start_n)
    return res == 0xDEADBEEF
}

import std/io::println

fn fib(n: i64) -> i64 {
    if n < 2 {
        return n
    }
    return fib(n - 1) + fib(n - 2)
}

fn main() -> i32 {
    auto start_n = 45:i64
    auto res = fib(start_n)
    if (res == 0xDEADBEEF) {
        println("never see me")
    }
    return 0
}
